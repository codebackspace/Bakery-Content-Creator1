[build]
  command = "pnpm install && pnpm --filter @workspace/bakery run build:netlify"
  publish = "artifacts/bakery/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
